El shell Bash

MacProgramadores

1. Redirecciones
En el apartado 4 del Tema 1 vimos los principales operadores de redirección:
>, <, >>, 2> y |. Realmente existen otros muchos operadores de redirección
que hemos postergado su explicación hasta ahora. Todos estos operadores
están resumidos en la Tabla 7.1.
Operador
cmd1|cmd2
>fichero
<fichero
>>fichero
>|fichero

<>fichero
n<>fichero
<<etiqueta
<<<texto
n>fichero
n<fichero
n>>fichero
n>|fichero

n>&
n<&
n>&m
n<&m
&>fichero
<&>&n>&n<&-

Descripción
Pipe; Coge la salida estándar de cmd1 y la envía a la entrada
estándar de cmd2.
Redirige la salida estándar del programa al fichero.
Redirige el contenido de fichero a la entrada estándar del
programa.
Redirige la salida estándar del programa al fichero. Añade
esta a fichero si éste ya existe.
Redirige la salida estándar del programa al fichero.
Sobrescribe a éste incluso si la opción noclobber está
activada.
Usa a fichero tanto para la entrada estándar como para la
salida estándar.
Usa a fichero tanto para la entrada como para la salida del
descriptor de fichero n.
Here document. Véase el apartado 1.3.
Here string. Véase el apartado 1.3.
Envía el valor del descriptor de fichero n al fichero.
Obtiene el valor del descriptor de fichero n del fichero.
Envía el valor del descriptor de fichero n al fichero. Añade
al final del fichero si éste existe.
Envía el valor del descriptor de fichero n al fichero.
Sobrescribe a éste incluso si la opción noclobber está
activada.
Enlaza la salida estándar en el descriptor de fichero n
Enalza la entrada estándar en el descriptor de fichero n
Enlaza el descriptor de salida m en el descriptor de fichero n
Enlaza el descriptor de entrada m en el descriptor de fichero n
Redirige la salida estándar y la salida de error estándar al
fichero
Cierra la entrada estándar
Cierra la salida estándar
Cierra el descriptor de salida n
Cierra el descriptor de entrada n

Tabla 7.1: Operadores de redirección

Pág 108

